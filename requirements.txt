discord.py[voice]>=2.4.0,<3.0.0
yt-dlp>=2025.1.1
# Solves YouTube's "n" signature challenge. Without it (plus a JS runtime)
# YouTube returns only storyboards and every download fails with
# "Requested format is not available".
yt-dlp-ejs>=0.8.0
python-dotenv>=1.0.1
